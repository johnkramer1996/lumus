const settingsSelectors = {
   getTypeShow: ({ settings }) => settings.typeShow,
   getFilter: ({ settings }) => settings.filter,
   getPage: ({ settings }) => settings.page,
}

export default settingsSelectors
